import {createTheme} from "@material-ui/core";

const theme = createTheme({
  palette: {
    type: "dark",
    primary: {
      main: '#1b5e20'
    },
    secondary: {
      main: '#fb8c00',
    },

  }
});

export default theme;
