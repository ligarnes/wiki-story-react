import {createTheme} from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: '#1b5e20'
    },
    secondary: {
      main: '#fb8c00',
    },
  }
});

export default theme;
